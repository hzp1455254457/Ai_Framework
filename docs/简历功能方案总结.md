# 简历优化和生成功能 - 可执行方案总结

## 📋 方案概述

本方案为AI框架添加完整的简历优化和生成功能，使用阿里通义千问大模型进行内容优化，并移除前端Agent页面，聚焦核心功能（对话、图片识别、简历优化）。

**OpenSpec提案ID**：`add-resume-optimization-and-generation`

**提案状态**：✅ 已创建并通过验证

---

## 🎯 核心功能

### 1. 简历解析模块
- **支持格式**：PDF、Word、JSON
- **解析技术**：
  - PDF：pdfplumber（保留布局信息）
  - Word：python-docx（提取段落和表格）
  - JSON：直接加载和验证
- **提取内容**：
  - 基本信息（姓名、邮箱、电话、地址、简介）
  - 教育经历
  - 工作经历
  - 项目经历
  - 技能
  - 证书
- **性能指标**：解析时间 < 5秒，最大文件10MB

### 2. 简历优化模块
- **使用模型**：阿里通义千问（qwen-max或qwen-plus）
- **优化级别**：
  - 基础优化：内容完整性、语法错误、格式规范
  - 高级优化：关键词优化、亮点提炼、语言表达优化
- **优化维度**：
  - 针对岗位描述优化（可选）
  - 提供优化建议列表
  - 提供简历评分（0-100）
- **性能指标**：优化时间 < 10秒

### 3. 简历生成模块
- **模板引擎**：Jinja2
- **PDF生成**：WeasyPrint（HTML转PDF）
- **输出格式**：HTML、PDF
- **专业模板**：
  1. 经典模板（传统格式，适合保守行业）
  2. 现代模板（简洁设计，适合互联网行业）
  3. 创意模板（个性化设计，适合设计类岗位）
  4. 技术模板（技术风格，适合技术岗位）
- **性能指标**：生成时间 < 5秒

### 4. 简历美化模块
- **模板管理**：统一管理所有简历模板
- **模板结构**：
  - HTML模板文件
  - CSS样式文件
  - 模板元数据（名称、描述、预览图）
- **易于扩展**：支持添加新模板

---

## 🏗️ 技术架构

### 后端架构

```
ResumeService (简历服务)
├── ResumeParser (简历解析器)
│   ├── PDF解析器 (pdfplumber)
│   ├── Word解析器 (python-docx)
│   └── JSON解析器
├── ResumeOptimizer (简历优化器)
│   └── 通义千问LLM集成
├── ResumeGenerator (简历生成器)
│   ├── Jinja2模板渲染
│   └── WeasyPrint PDF转换
└── ResumeTemplate (模板管理器)
    └── 4个专业模板
```

### 前端架构

```
Resume.vue (简历页面)
├── ResumeUpload.vue (文件上传)
├── ResumePreview.vue (解析结果展示)
├── ResumeOptimization.vue (优化区域)
├── TemplateSelector.vue (模板选择)
└── ResumeExport.vue (导出区域)
```

### API接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/v1/resume/parse | 解析简历文件 |
| POST | /api/v1/resume/optimize | 优化简历内容 |
| POST | /api/v1/resume/generate | 生成简历文件 |
| GET | /api/v1/resume/templates | 列出可用模板 |
| GET | /api/v1/resume/download/{file_id} | 下载生成的文件 |
| GET | /api/v1/resume/preview/{file_id} | 预览生成的文件 |

---

## 🔧 技术选型

### 后端依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| dashscope | >=1.14.0 | 通义千问API（已有） |
| PyPDF2 | 3.0.1 | PDF解析 |
| pdfplumber | 0.10.3 | PDF解析（保留布局） |
| python-docx | 1.1.0 | Word文档解析 |
| WeasyPrint | 60.1 | HTML转PDF |
| Jinja2 | 3.1.2 | 模板渲染 |

### 前端依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| pdfjs-dist | ^3.11.174 | PDF预览 |
| quill | ^1.3.7 | 富文本编辑（可选） |

### 配置示例

```yaml
resume:
  # LLM配置（使用通义千问）
  llm:
    provider: "qwen"
    model: "qwen-max"  # 或 qwen-plus
    temperature: 0.7
    max_tokens: 2000
  
  # 解析配置
  parser:
    max_file_size: 10485760  # 10MB
    supported_formats: ["pdf", "docx", "json"]
  
  # 生成配置
  generator:
    output_dir: "data/resumes/generated"
    pdf_engine: "weasyprint"
    default_template: "modern"
  
  # 模板配置
  templates:
    dir: "core/resume/templates"
    available: ["classic", "modern", "creative", "tech"]
```

---

## 📱 前端页面调整

### 移除内容
- ❌ 删除 `src/views/Agent.vue`
- ❌ 删除 `src/api/endpoints/agent.ts`
- ❌ 删除 `src/stores/agent.ts`
- ❌ 从路由中移除Agent相关配置
- ❌ 从导航菜单中移除Agent入口

### 保留内容
- ✅ 保留 `Chat.vue`（对话功能）
- ✅ 保留 `Vision.vue`（图片识别功能）

### 新增内容
- ✅ 新增 `Resume.vue`（简历优化功能）
- ✅ 新增 Resume 相关组件（Upload、Preview、Optimization、TemplateSelector、Export）
- ✅ 新增 Resume API客户端
- ✅ 新增 Resume 状态管理（Pinia store）
- ✅ 新增 Resume 路由配置

---

## 📊 实施计划

### Phase 1: 后端核心模块（1周）
1. 实现ResumeService基础框架
2. 实现ResumeParser（支持PDF、Word、JSON）
3. 实现ResumeOptimizer（集成通义千问）
4. 定义数据模型和API接口

**角色**：`infrastructure-developer`、`llm-service-developer`

### Phase 2: 简历生成和模板（1周）
1. 实现ResumeGenerator（HTML渲染 + PDF转换）
2. 实现ResumeTemplate管理器
3. 创建4个基础模板（经典、现代、创意、技术）
4. 实现模板预览和选择

**角色**：`infrastructure-developer`

### Phase 3: API和路由（3天）
1. 实现Resume API路由
2. 实现文件上传和下载
3. 实现API依赖注入
4. 编写API单元测试

**角色**：`api-developer`、`ai-framework-qa-engineer`

### Phase 4: 前端页面（1周）
1. 移除Agent页面和路由
2. 创建Resume页面和子组件
3. 实现文件上传和解析
4. 实现优化建议展示
5. 实现模板选择和预览
6. 实现导出和下载

**角色**：`ai-framework-frontend-developer`

### Phase 5: 测试和文档（3天）
1. 编写单元测试（覆盖率80%+）
2. 编写集成测试
3. 更新API文档
4. 更新用户指南
5. 更新项目计划

**角色**：`ai-framework-qa-engineer`、`ai-framework-documenter`

**总计**：约3-4周，114个任务

---

## 📈 性能指标

| 指标 | 目标值 |
|------|--------|
| 简历解析时间 | < 5秒 |
| 简历优化时间 | < 10秒（含LLM调用） |
| 简历生成时间 | < 5秒 |
| API响应时间 | < 1秒（不含LLM调用） |
| API成功率 | > 99% |
| 最大文件大小 | 10MB |
| 单元测试覆盖率 | > 80% |

---

## 🔒 安全性考虑

### 文件上传安全
- 验证文件类型和大小
- 隔离上传文件存储
- 防止路径遍历攻击

### 数据隐私
- 简历数据加密存储
- 定期清理临时文件
- 不记录敏感信息到日志

### API安全
- 请求限流
- 文件访问权限控制
- 防止恶意文件上传

---

## 🎯 成功标准

### 功能完整性
- [x] 支持PDF、Word、JSON格式解析
- [x] 基于通义千问的内容优化
- [x] 提供至少4个专业模板
- [x] 支持HTML和PDF导出
- [x] 前端页面完整（上传、优化、模板、导出）

### 质量指标
- [x] 单元测试覆盖率 > 80%
- [x] API响应成功率 > 99%
- [x] 用户满意度 > 4.0/5.0

---

## 📚 相关文档

- **OpenSpec提案**：`openspec/changes/add-resume-optimization-and-generation/proposal.md`
- **设计文档**：`openspec/changes/add-resume-optimization-and-generation/design.md`
- **任务清单**：`openspec/changes/add-resume-optimization-and-generation/tasks.md`
- **规格文档**：
  - `openspec/changes/add-resume-optimization-and-generation/specs/resume-service/spec.md`
  - `openspec/changes/add-resume-optimization-and-generation/specs/api-server/spec.md`

---

## 🚀 下一步行动

### 立即执行
1. ✅ 创建OpenSpec提案（已完成）
2. ⏳ 移除前端Agent页面，保留Chat和Vision
3. ⏳ 实现后端简历服务模块
4. ⏳ 实现前端Resume页面
5. ⏳ 编写测试和文档

### 验证提案
```bash
cd F:\Ai_Framework
openspec validate add-resume-optimization-and-generation --strict
```

**验证结果**：✅ 已通过验证

### 开始实施
```bash
# 查看提案详情
openspec change show add-resume-optimization-and-generation

# 开始实施（需要用户批准）
# 按照tasks.md中的任务清单逐步实施
```

---

## 💡 关键决策

### 为什么选择通义千问？
1. **用户要求**：明确要求"尽量使用阿里系的"
2. **成本优势**：相比OpenAI GPT-4成本更低
3. **中文优化**：对中文简历优化效果更好
4. **已有集成**：项目已集成通义千问，无需额外配置

### 为什么移除Agent页面？
1. **简化界面**：聚焦核心功能，提升用户体验
2. **垂直场景**：从通用AI工具转向垂直场景应用
3. **功能重复**：Agent功能可以通过Chat页面实现

### 为什么使用WeasyPrint？
1. **CSS支持**：完整支持CSS样式，排版美观
2. **易于维护**：基于HTML模板，易于修改和扩展
3. **性能良好**：转换速度快，满足性能要求

---

**方案创建日期**：2026-01-28
**方案创建人**：AI Framework Architect
**最后更新日期**：2026-01-28
