# 生产依赖

# HTTP客户端
httpx>=0.24.0

# Web框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 配置文件解析
PyYAML>=6.0

# 环境变量管理
python-dotenv>=1.0.0

# 数据验证（FastAPI依赖）
pydantic>=2.5.0

# 日志管理（可选，当前使用标准库logging）
# loguru>=0.7.0

# 重试机制
tenacity>=8.2.0

# 异步文件操作
aiofiles>=23.0.0

# 异步SQLite驱动
aiosqlite>=0.19.0

# Token计算（用于LLM精确计算）
tiktoken>=0.12.0

# 加密库（用于API密钥加密存储）
cryptography>=41.0.0

# HTML解析库（用于互联网访问工具）
beautifulsoup4>=4.12.0

# 可选依赖：主流AI框架集成
# LiteLLM - 统一多模型接口框架（可选）
# litellm>=1.0.0

# LangChain - AI应用开发框架（可选）
# langchain>=0.1.0

# LangGraph - 工作流编排框架（可选）
# langgraph>=0.0.1

# Prometheus客户端（监控指标）
prometheus-client>=0.19.0